# 开发：Excellent（11964948@qq.com）

## 查询意图映射手册

### 意图到资产类型
- 怎么做：优先命中 `playbook` 与 `checklist`。
- 为什么：优先命中 `standard` 与 `case`。
- 出故障了：优先命中 `checklist`、`playbook`、`antipattern`。
- 方案对比：优先命中 `case` 与 `standard`。
- 术语解释：优先命中 `glossary`。

### 查询增强规则
- 包含“回滚、恢复、故障”关键词时，提高发布与运维类条目权重。
- 包含“慢、超时、抖动”关键词时，提高性能与数据库类条目权重。
- 包含“越权、泄露、密钥”关键词时，提高安全类条目权重。
- 包含“重构、迁移、兼容”关键词时，提高重构与API治理权重。
