apiVersion: v1
kind: Service
metadata:
  name: super-dev
  labels:
    app: super-dev
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: super-dev
