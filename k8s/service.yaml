apiVersion: v1
kind: Service
metadata:
  name: 构建一个宠物护理预约平台-先做前端
  labels:
    app: 构建一个宠物护理预约平台-先做前端
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: 构建一个宠物护理预约平台-先做前端
