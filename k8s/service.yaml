apiVersion: v1
kind: Service
metadata:
  name: 用户认证系统
  labels:
    app: 用户认证系统
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: 用户认证系统
