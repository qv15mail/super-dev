apiVersion: v1
kind: Secret
metadata:
  name: super-dev-secret
type: Opaque
stringData:
  database-url: "postgresql://postgres:REPLACE_IN_CLUSTER@postgres:5432/super_dev"
  jwt-secret: "REPLACE_IN_CLUSTER"
  api-key: "REPLACE_IN_CLUSTER"
