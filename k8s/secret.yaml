apiVersion: v1
kind: Secret
metadata:
  name: 构建一个宠物护理预约平台-先做前端-secret
type: Opaque
stringData:
  database-url: "postgresql://user:password@postgres:5432/构建一个宠物护理预约平台-先做前端"
  jwt-secret: "your-jwt-secret-here"
  api-key: "your-api-key-here"
